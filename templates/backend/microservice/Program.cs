using H2OMicroService;

namespace --ProjectFullName--
{
    public static class Program
    {
        public static void Main(string[] args)
        {
            var micro = new MicroService();
            micro.Run(args);
        }
    }
}
